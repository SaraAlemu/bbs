Quick start — one command to run the app (Windows)

1) Open PowerShell in the project folder:
   cd "C:\Users\YONI-TECH\Downloads\Crop-Recommendation-Using-Soil-Type-main\Crop-Recommendation-Using-Soil-Type-main"

2) Run (PowerShell):
   .\run.ps1

Notes:
- `run.ps1` will create `.venv` and install dependencies only if the venv doesn't exist.
- If PowerShell blocks script execution, run:
  powershell -ExecutionPolicy Bypass -NoProfile -File .\run.ps1
- For a GUI option, double-click `run.bat`.
